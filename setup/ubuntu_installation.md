 Ubuntu Installation & Packages

 Environment
Ubuntu Desktop was installed inside VMware Workstation to serve as the honeypot server.

 Installed Packages
The following packages were installed to support OpenCanary and Samba:

```bash
sudo apt update
sudo apt install python3-pip python3-venv samba smbclient -y

After installation, the system confirmed the latest versions were installed

This ensured the honeypot environment was ready for configuration.
